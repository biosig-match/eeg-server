# Global environment variables for the EEG Server project

# --- RabbitMQ Settings ---
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_HOST=rabbitmq

# --- PostgreSQL/TimescaleDB Settings ---
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=erp_data
POSTGRES_HOST=db

# --- Service Ports ---
# Ports exposed to the host machine
NGINX_PORT=8080
PGADMIN_PORT=5050
RABBITMQ_MGMT_PORT=15672

# --- Issue #4 Migration Flags ---
# Collector publish format to RabbitMQ: json | bin | both
# Default to json to avoid breaking existing Processor until v2 is verified.
COLLECTOR_MQ_FORMAT=json

# Processor consumer mode: v1 | v2 | both
# Default to v1 for Phase 0; switch to v2 after Collector migration.
PROCESSOR_MQ_FORMAT=v1
