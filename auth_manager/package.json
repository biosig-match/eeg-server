{
  "name": "auth-manager",
  "version": "1.0.0",
  "description": "A service to manage authorization (participants and roles) for experiments.",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch --env-file=../.env run src/index.ts"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.4.11",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6",
    "bun-types": "1.2.23",
    "typescript": "^5.4.5"
  }
}
