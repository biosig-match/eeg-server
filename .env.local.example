# Copy to `.env.local` and adjust secrets before running docker compose locally.
# These values mirror the shared defaults but allow you to keep credentials out of version control.

# NOTE: 開発用のサンプル値です。本番運用の前に必ず安全な値へ変更してください。
POSTGRES_PASSWORD=password
# NOTE: 開発用のサンプル値です。本番運用の前に必ず安全な値へ変更してください。
RABBITMQ_PASSWORD=guest
# NOTE: 開発用のサンプル値です。本番運用の前に必ず安全な値へ変更してください。
OBJECT_STORAGE_SECRET_KEY=storageadmin
# NOTE: 開発用のサンプル値です。本番運用の前に必ず安全な値へ変更してください。
GEMINI_API_KEY=sample-local-key

# Uncomment to override service ports for local developers.
# NGINX_PORT=8080
# RABBITMQ_MGMT_PORT=15672
