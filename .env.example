Example environment variables for the EEG Server project
Copy this file to .env and fill in the values for your local development environment.
--- RabbitMQ Settings ---
Credentials for RabbitMQ server
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

Hostname of the RabbitMQ container
RABBITMQ_HOST=rabbitmq

--- PostgreSQL/TimescaleDB Settings ---
Credentials for PostgreSQL server
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=eeg_data

Hostname of the PostgreSQL container
POSTGRES_HOST=db

--- MinIO Settings ---
Hostname of the MinIO container
MINIO_ENDPOINT=minio

Port for the MinIO API
MINIO_PORT=9000

Port for the MinIO Console UI
MINIO_CONSOLE_PORT=9001

Credentials for MinIO server
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

Set to true if MinIO is configured with TLS/SSL
MINIO_USE_SSL=false

Bucket name for storing raw, compressed sensor data
MINIO_RAW_DATA_BUCKET=raw-data

Bucket name for storing media files (images, audio)
MINIO_MEDIA_BUCKET=media

--- Service Ports ---
Ports exposed to the host machine for access from your local computer
e.g., access Nginx at http://localhost:8080
NGINX_PORT=8080

e.g., access pgAdmin at http://localhost:5050
PGADMIN_PORT=5050

e.g., access RabbitMQ Management UI at http://localhost:15672
RABBITMQ_MGMT_PORT=15672Example environment variables for the EEG Server project
Copy this file to .env and fill in the values for your local development environment.
--- RabbitMQ Settings ---
Credentials for RabbitMQ server
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

Hostname of the RabbitMQ container
RABBITMQ_HOST=rabbitmq

--- PostgreSQL/TimescaleDB Settings ---
Credentials for PostgreSQL server
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=eeg_data

Hostname of the PostgreSQL container
POSTGRES_HOST=db

--- MinIO Settings ---
Hostname of the MinIO container
MINIO_ENDPOINT=minio

Port for the MinIO API
MINIO_PORT=9000

Port for the MinIO Console UI
MINIO_CONSOLE_PORT=9001

Credentials for MinIO server
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin

Set to true if MinIO is configured with TLS/SSL
MINIO_USE_SSL=false

Bucket name for storing raw, compressed sensor data
MINIO_RAW_DATA_BUCKET=raw-data

Bucket name for storing media files (images, audio)
MINIO_MEDIA_BUCKET=media

--- Service Ports ---
Ports exposed to the host machine for access from your local computer
e.g., access Nginx at http://localhost:8080
NGINX_PORT=8080

e.g., access pgAdmin at http://localhost:5050
PGADMIN_PORT=5050

e.g., access RabbitMQ Management UI at http://localhost:15672
RABBITMQ_MGMT_PORT=15672